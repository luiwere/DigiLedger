-- Create the two databases used by LejaSmart.
-- If your hosting provider only exposes one DB, set OWNER_DATABASE_URL to the same URL as DATABASE_URL.

CREATE DATABASE lejasmart;
CREATE DATABASE lejasmart_owner;
